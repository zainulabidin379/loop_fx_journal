name: loop_fx_journal
description: "LOOP — Offline forex and gold trading journal."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  get_it: ^9.2.1
  go_router: ^17.3.0
  local_auth: ^3.0.2
  fl_chart: ^1.2.0
  intl: ^0.20.2
  uuid: ^4.5.1
  google_fonts: ^8.1.0
  flutter_svg: ^2.0.17
  image_picker: ^1.1.2
  file_picker: ^11.0.2
  path_provider: ^2.1.5
  share_plus: ^10.1.4
  flutter_local_notifications: ^22.0.1
  timezone: ^0.11.1
  hugeicons: ^1.1.7
  auto_size_text: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: 
  build_runner: 

flutter:
  uses-material-design: true
  assets:
    - assets/images/
